## Do these things when running development on a sprite:

1. Set the CLOUDFLARE_DATABASE_ID to the proper value (see `wrangler.jsonc`).
2. Run `npm install` in this directory, and in the `line-of-time-fe` directory.
3. Run `./build-schema-update.sh` to set up the database.
