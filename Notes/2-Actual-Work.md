Go ahead and implement the plan in plan.md to extract the display of the Timeline from the HomeView.vue UI element, to its own TimelineDisplay.vue UI element. The props idea is the right thing to do.
